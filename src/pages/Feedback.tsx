import SuggestionCard from "../components/shared/SuggestionCard";

const Feedback = () => {
  return (
    <main className="mt-6">
        <SuggestionCard />
    </main>
  );
};

export default Feedback;
